namespace OnlineLearningPlatform.Models
{

    public enum UserRole { Student, Instructor, Admin }
    public enum EnrollmentStatus { Active, Completed, Dropped }
    public enum LessonType { Video, Text }
    public enum SourceType { Manual, AI }
    public enum QuestionType { MCQ, MSQ, TrueFalse, ShortAnswer }
}
